import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import { createPageUrl } from '@/utils';
import { motion } from 'framer-motion';
import { Check, ArrowRight, Zap, Feather, Shield, Droplets } from 'lucide-react';

export default function ProductHighlight() {
  const [activeSpec, setActiveSpec] = useState(0);

  const specs = [
    {
      icon: Zap,
      title: "Explosive Traction",
      value: "12-point",
      description: "Precision-molded studs from recycled TPU deliver unmatched grip on any surface"
    },
    {
      icon: Feather,
      title: "Ultra-Light",
      value: "185g",
      description: "One of the lightest performance cleats on the market, without sacrificing durability"
    },
    {
      icon: Shield,
      title: "Reinforced Toe",
      value: "3-Layer",
      description: "Triple-reinforced strike zone for powerful shots and precise ball control"
    },
    {
      icon: Droplets,
      title: "Water Resistant",
      value: "IPX4",
      description: "Hydrophobic coating keeps you light and fast in wet conditions"
    }
  ];

  const materials = [
    "Upper: 100% recycled ocean plastic yarns",
    "Sole plate: Recycled carbon fiber composite",
    "Studs: Post-consumer recycled TPU",
    "Lining: Algae-based foam (Bloom™)",
    "Laces: Recycled polyester blend"
  ];

  return (
    <section className="py-24 lg:py-32 bg-gray-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        {/* Header */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="text-center max-w-3xl mx-auto mb-16"
        >
          <span className="text-sm font-semibold text-[#00F5D4] tracking-wider uppercase mb-4 block">
            Flagship Product
          </span>
          <h2 className="text-3xl sm:text-4xl lg:text-5xl font-bold text-[#0A2647] mb-6">
            Introducing the Eco-Cleat
          </h2>
          <p className="text-lg text-gray-600">
            The culmination of 3 years of R&D. Professional-grade performance, 
            100% sustainable materials.
          </p>
        </motion.div>

        {/* Main Content */}
        <div className="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
          {/* Image */}
          <motion.div
            initial={{ opacity: 0, x: -30 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            className="relative order-2 lg:order-1"
          >
            <div className="relative aspect-square rounded-3xl overflow-hidden bg-white shadow-2xl">
              <img
                src="https://images.unsplash.com/photo-1579952363873-27f3bade9f55?w=800&q=80"
                alt="Eco-Cleat Detail"
                className="w-full h-full object-cover"
              />
              
              {/* Price Tag */}
              <div className="absolute bottom-6 left-6 right-6 bg-white/95 backdrop-blur-sm rounded-2xl p-6 shadow-lg">
                <div className="flex items-center justify-between">
                  <div>
                    <div className="text-sm text-gray-500 line-through">$249</div>
                    <div className="text-3xl font-bold text-[#0A2647]">$120</div>
                  </div>
                  <Link
                    to={createPageUrl('Product') + '?slug=eco-cleat'}
                    className="inline-flex items-center gap-2 px-6 py-3 bg-[#0A2647] text-white font-semibold rounded-xl hover:bg-[#144272] transition-colors"
                  >
                    Shop Now
                    <ArrowRight className="w-4 h-4" />
                  </Link>
                </div>
              </div>
            </div>

            {/* Floating Sustainability Badge */}
            <motion.div
              initial={{ opacity: 0, scale: 0.8 }}
              whileInView={{ opacity: 1, scale: 1 }}
              viewport={{ once: true }}
              transition={{ delay: 0.3 }}
              className="absolute -top-4 -right-4 w-24 h-24 bg-[#00F5D4] rounded-full flex flex-col items-center justify-center shadow-lg"
            >
              <div className="text-2xl font-bold text-[#0A2647]">15</div>
              <div className="text-xs text-[#0A2647]/80 text-center leading-tight">bottles<br/>saved</div>
            </motion.div>
          </motion.div>

          {/* Specs */}
          <motion.div
            initial={{ opacity: 0, x: 30 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            className="order-1 lg:order-2"
          >
            <h3 className="text-2xl font-bold text-[#0A2647] mb-8">Technical Specifications</h3>
            
            {/* Spec Cards */}
            <div className="grid grid-cols-2 gap-4 mb-10">
              {specs.map((spec, index) => (
                <button
                  key={spec.title}
                  onClick={() => setActiveSpec(index)}
                  className={`p-5 rounded-2xl text-left transition-all duration-300 ${
                    activeSpec === index
                      ? 'bg-[#0A2647] shadow-lg scale-105'
                      : 'bg-white hover:shadow-md'
                  }`}
                >
                  <spec.icon className={`w-6 h-6 mb-3 ${
                    activeSpec === index ? 'text-[#00F5D4]' : 'text-[#0A2647]'
                  }`} />
                  <div className={`text-2xl font-bold mb-1 ${
                    activeSpec === index ? 'text-[#00F5D4]' : 'text-[#0A2647]'
                  }`}>
                    {spec.value}
                  </div>
                  <div className={`text-sm font-medium ${
                    activeSpec === index ? 'text-white' : 'text-gray-600'
                  }`}>
                    {spec.title}
                  </div>
                </button>
              ))}
            </div>

            {/* Active Spec Description */}
            <motion.div
              key={activeSpec}
              initial={{ opacity: 0, y: 10 }}
              animate={{ opacity: 1, y: 0 }}
              className="p-6 bg-white rounded-2xl mb-10"
            >
              <p className="text-gray-600 leading-relaxed">
                {specs[activeSpec].description}
              </p>
            </motion.div>

            {/* Materials */}
            <div className="bg-[#0A2647] rounded-2xl p-6">
              <h4 className="text-lg font-semibold text-white mb-4">Sustainable Materials</h4>
              <ul className="space-y-3">
                {materials.map((material, index) => (
                  <li key={index} className="flex items-start gap-3">
                    <Check className="w-5 h-5 text-[#00F5D4] mt-0.5 flex-shrink-0" />
                    <span className="text-white/80 text-sm">{material}</span>
                  </li>
                ))}
              </ul>
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
